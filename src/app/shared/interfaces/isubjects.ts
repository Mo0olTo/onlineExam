export interface Isubjects {
  _id: string;
  name: string;
  icon: string;
  createdAt: string;
}


interface RootObject {

}

interface Subject {

}

interface Metadata {
  currentPage: number;
  numberOfPages: number;
  limit: number;
}